name: CI/CD

on:
  pull_request:
    branches: [master]
  push:
    branches: [master]
permissions:
  contents: read
  packages: write

jobs:
  pipeline:
    uses: PrintNow-Dev/PrintNow/.github/workflows/backend.yml@dev

    with:
      service: "printnow-document-service"
      image: "ghcr.io/printnow-dev/printnow-document-service"
      trigger: ${{ github.event_name }}

    secrets: inherit